{
  "name": "pix-api-python",
  "version": "1.0.0",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 30",
    "health-check": "curl -s http://localhost:$PORT/health || exit 1"
  },
  "dependencies": {
    "uvicorn": "^0.22.0"
  }
}